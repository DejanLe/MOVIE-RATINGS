 <%= comment.name %>
<%= comment.body %>
<%= time_ago_in_words(comment.created_at) %>
<small><b>ago</b></small> <br>	

<% if user_signed_in? %>
    <% if @rating.user_id == current_user.id %>
    <%= link_to "Edit"  %>
    <%= link_to "Delete", [comment.rating, comment], method: :delete,  data: { confirm: 'Are you sure?' } %>
    <% end %>
<% end %>

